CandleAnatomy from stock-market-explainer-web. Use via `window.StocksDS.CandleAnatomy` (bundle loaded from the root `_ds_bundle.js`). Components expect the context this repo's `.storybook/preview` decorators provide (theme/i18n) — see README.md.

Variants (see `CandleAnatomy.html`): Default.

## Examples

```jsx
// Default
export const Default: Story = {};
```

### Default

```jsx
/* Default */ compose(S, "Default")
```
