# Secrets
.env
.env.local
*.key

# Node / Next.js
node_modules/
.next/
out/
dist/
npm-debug.log*
yarn-error.log*
# Auto-generated by Next/TS on every build — should never be tracked.
next-env.d.ts
*.tsbuildinfo
# Self-signed dev TLS certs (npm run dev:https) — machine-local, never commit the key.
certificates/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# OS / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Misc
*.log
coverage/

# Deploy — real inventory may hold a username; keep only the .example
deploy/ansible/inventory.ini
bench/results/
